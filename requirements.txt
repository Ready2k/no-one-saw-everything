# ── Async HTTP ────────────────────────────────────────────────────────────────
aiohttp>=3.9.0            # 3.8.x has asyncio compat issues on Python 3.12
aiosignal>=1.3.1
async-generator>=1.10
async-timeout>=4.0.2
frozenlist>=1.3.3
multidict>=6.0.4
yarl>=1.9.0

# ── AWS ───────────────────────────────────────────────────────────────────────
boto>=2.49.0
botocore>=1.29.43
s3transfer>=0.6.0
jmespath>=1.0.1

# ── Web framework (frontend server) ───────────────────────────────────────────
asgiref>=3.7.0            # Django 4.2 LTS minimum
Django>=4.2,<5.0          # Django 2.2 is EOL & broken on 3.12; 4.2 is LTS
django-cors-headers>=4.3.0
django-storages>=1.14     # django-storages-redux is a dead fork
dj-database-url>=2.1.0   # 0.5.0 has broken URL API with Django 4.x
gunicorn>=20.1.0
psycopg2-binary>=2.9.5
sqlparse>=0.4.4

# ── Core AI / ML ──────────────────────────────────────────────────────────────
openai==0.27.0            # pinned — gpt_structure.py uses the 0.x API
numpy>=1.25.2,<2.0
scipy>=1.11.1
scikit-learn>=1.3.0
scikit-posthocs>=0.7.0
# sklearn==0.0 removed — deprecated stub; scikit-learn already listed above
pandas>=2.0.3
gensim>=4.3.2             # 3.x uses collections.Mapping removed in Python 3.10
smart-open>=6.0.0         # gensim dependency; 5.x has 3.12 issues
sentence-transformers>=2.7  # local embeddings (replaces openai Embedding API)

# ── NLP ───────────────────────────────────────────────────────────────────────
nltk>=3.6.5
regex>=2022.0.0

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.7.2
seaborn>=0.12.2
yellowbrick>=1.5

# ── Stats ─────────────────────────────────────────────────────────────────────
statsmodels>=0.13.5
trueskill>=0.4.5
patsy>=0.5.3

# ── Images ────────────────────────────────────────────────────────────────────
Pillow>=10.0.0

# ── Browser automation ────────────────────────────────────────────────────────
selenium>=4.8.2
trio>=0.22.0
trio-websocket>=0.9.2
wsproto>=1.2.0
outcome>=1.2.0
sortedcontainers>=2.4.0

# ── Utilities ─────────────────────────────────────────────────────────────────
requests>=2.28.0
certifi>=2024.0.0
charset-normalizer>=3.0.0
idna>=3.4
urllib3>=1.26.18          # security patch over 1.26.7
six>=1.16.0
click>=8.0.3
python-dateutil>=2.8.2
pytz>=2023.3
packaging>=23.0
pyparsing>=3.0.6
cycler>=0.11.0
kiwisolver>=1.4.4
joblib>=1.1.1
threadpoolctl>=3.0.0
tqdm>=4.62.3
PySocks>=1.7.1
h11>=0.14.0
sniffio>=1.3.0
pycparser>=2.21
exceptiongroup>=1.1.0

# ── Typing / metadata ─────────────────────────────────────────────────────────
typing-extensions>=4.8.0  # 4.0.0 too old for modern deps
# importlib-metadata pin removed — Python 3.12 ships it as a stdlib module
